#!/usr/bin/env bash
set -e
DIR="$(cd "$(dirname "$0")" && pwd)"

# Auto-detect or create venv
if [ ! -f "$DIR/.venv/bin/python3" ]; then
    python3 -m venv "$DIR/.venv"
    "$DIR/.venv/bin/pip" install pyyaml -q
fi

exec "$DIR/.venv/bin/python3" "$DIR/build.py" "$@"
